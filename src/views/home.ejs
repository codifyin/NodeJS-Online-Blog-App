<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Welcome to Blogging Platform!</h1>
    <ul>
        <% blogs.forEach(blog => { %>
            <li><%= blog.title %> - <%= blog.body %> - <%= blog.author %></li>
        <% }); %>
    </ul>
    <a href="/logout">Logout</a>
</body>
</html>
